name: Dev PR
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml

jobs:        
  r-packages:
    runs-on: ${{ true && 'ubuntu-latest' || 'macos-latest' }}
    steps:
      - run: echo "ubuntu"
  r-packages2:
    runs-on: ${{ false && 'ubuntu-latest' || 'macos-latest' }}
    steps:
      - run: echo "macos?"
