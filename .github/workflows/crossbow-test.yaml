name: Crossbow
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml



jobs:        
  r-packages:
    runs-on: macos-latest
    steps:
      - run: |
          echo ':open_book: You can find the preview here https://crossbow.voltrondata.com/pr_docs/13913' >> $GITHUB_STEP_SUMMARY

