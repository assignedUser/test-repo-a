name: Dev PR
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml

jobs:        
  step1:
    runs-on: ubuntu-latest
    steps:
      - env:
          pr_ids: (1 2 3)
        run: |
          for id in ${pr_ids[@]}
          do
            echo $id
          done
