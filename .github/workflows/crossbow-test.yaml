name: Crossbow
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml



jobs:        
  r-packages:
    runs-on: ubuntu-latest
    steps:
     - name: Get sccache
       shell: bash
       run: free -h
