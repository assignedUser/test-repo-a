name: dispatch-test

on:
  push:
    branches: [main]
  workflow_dispatch:
  pull_request:
  
jobs:
  test:
    runs-on: windows-latest
   
    steps:
      - id: s1
        run: |
         "/a/new/path" | Add-Content -path $GITHUB_PATH;
         cat $GITHUB_PATH;
      - run: echo $PATH 
 

