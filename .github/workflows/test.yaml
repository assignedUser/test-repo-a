name: dispatch-test

on:
  workflow_dispatch:
  pull_request:
  
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      SUPER_SECRET: "Mona The Octocat"
    steps:
      - uses: actions/checkout@v2
      - name: Trigger pkgcheck-action build
        run: |
          echo "::add-mask::$SUPER_SECRET"
          echo "Mona"
          echo  "Mona The Octocat"
          echo  "::notice::\033[1m\033[22masdasd%0Aasdasd%0Aasdasd"
          echo "::warning file=test.R,line=1,endLine=3::Octolog makes it%25 easy to create beautiful output in Github Action logs%0A%0A and use workflow commands in R.!"
        shell: bash
